<?php foreach($messages as $message): ?>
    <?php echo view('page::profile/messenger/message', array('message' => $message, 'mark' => isset($mark) ? $mark : true, 'user_id' => isset($user_id) ? $user_id : get_userid(), 'conversation' => $conversation)) ?>
<?php endforeach ?>
